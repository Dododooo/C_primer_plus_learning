#include<stdio.h>
int ret_max(int *arr,int n)
{
    int index;
    int max=index[0];
    for (index=0;index<n;index++)
    {
        if(arr[index]>max)
        {
            max=arr[index];
        }
        
    }
    return max;
}